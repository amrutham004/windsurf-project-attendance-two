# Frontend API Configuration
# Empty string = use relative URLs so requests go through Vite's HTTPS proxy
# The proxy in vite.config.ts forwards /api/* and /mark-attendance to http://localhost:8000
VITE_API_URL=
